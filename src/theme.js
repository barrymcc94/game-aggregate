import {createMuiTheme} from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        type: 'light',
    },
    headerHeight: '4rem',
    footerHeight: '4rem',
    footerHeightSm: '5rem',
});

export default theme;