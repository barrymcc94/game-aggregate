import {createMuiTheme} from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        type: 'dark',
    },
    headerHeight: '4rem',
    footerHeight: '4rem',
    footerHeightSm: '5rem',
});

export default theme;