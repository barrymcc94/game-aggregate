import styled from 'styled-components';
import Typography from '@mui/material/Typography';

export const StyledGameFooter = styled.footer`
    text-align: right;
`;

export const FooterText = styled(Typography)``;
